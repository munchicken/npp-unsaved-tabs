#pragma once

// Custom command IDs for the Unsaved Tabs plugin
// Each ID corresponds to one command in the Plugins menu.

enum {
    IDM_CMD_SHOW_UNSAVED_TABS = 0,
    // future commands go here...
};
